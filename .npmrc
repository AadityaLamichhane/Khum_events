# Ignore build scripts for optional native modules
# This prevents compilation errors with sha3, scrypt, and other old native modules
ignore-scripts=true

# Allow optional dependencies to fail gracefully
legacy-peer-deps=false
